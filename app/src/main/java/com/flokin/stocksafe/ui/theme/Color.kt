package com.flokin.stocksafe.ui.theme

import androidx.compose.ui.graphics.Color

// Palet warna utama StockSafe (biru-teal, berbeda dengan modul employee)
val Teal80    = Color(0xFF80CBC4)
val TealGrey80 = Color(0xFFB2DFDB)
val Cyan80    = Color(0xFF80DEEA)

val Teal40    = Color(0xFF00897B)
val TealGrey40 = Color(0xFF4DB6AC)
val Cyan40    = Color(0xFF00ACC1)

// Warna surface
val SurfaceLight = Color(0xFFF5FAFA)
val SurfaceDark  = Color(0xFF121A1A)

// Warna status stok
val StockOk      = Color(0xFF43A047)  // hijau: stok aman
val StockLow     = Color(0xFFE65100)  // oranye: stok menipis
val StockOut     = Color(0xFFB71C1C)  // merah: stok habis

// Warna tipe transaksi
val StockIn      = Color(0xFF1976D2)  // biru: stok masuk
val StockOut2    = Color(0xFFD32F2F)  // merah: stok keluar

// Warna error
val ErrorLight   = Color(0xFFB00020)
val ErrorDark    = Color(0xFFCF6679)